---
id: 7.6
title: Advanced Test Backfill (Triage Fallbacks & Concurrency)
type: issue
status: ready-for-agent
labels: [enhancement, ready-for-agent, qa, testing, triage, concurrency]
---

> *This was generated by AI during triage.*

## Parent
[.scratch/project-vector/7.0-qa-robustness.md](./7.0-qa-robustness.md)

## Triage Notes
**What we've established so far:**
- The core implementation (7.1-7.5) is complete, including migration v10, Pydantic models, bot-detection, and retention logic.
- We need to backfill tests to reach >90% coverage for core modules.
- Specific focus is needed on:
    - `sorter.py`: Missing/malformed Doctrine files.
    - `database.py`: Transactional integrity under concurrent load.
    - `models.py`: Pydantic enforcement of LLM outputs.

## Agent Brief

### Context
This task focuses on strengthening the automated test suite following the robustness refactor. The system now uses Pydantic models for data flow and has a unified state machine.

### Objectives
1. **Sorter Robustness:** Update `tests/test_sorter.py` to simulate missing `DOCTRINE.md` or malformed YAML. Ensure the system handles these gracefully (e.g., defaulting to safety or logging an error).
2. **Database Concurrency:** Add tests to `tests/test_database.py` that use threading or multiprocessing to hit `upsert_job` and `log_action` simultaneously. Verify that SQLite's locking/transactions prevent data corruption or lost updates.
3. **Model Validation:** Add unit tests to verify that `TriageResult` and `EvaluationResult` correctly reject invalid types or out-of-range scores (e.g., score > 100).
4. **Coverage Audit:** Use `pytest-cov` to identify gaps in `sorter.py`, `database.py`, and `collector.py` and fill them.

### Target Files
- `tests/test_sorter.py`
- `tests/test_database.py`
- `tests/test_models.py` (create if needed)

### Verification
- Run `pytest --cov=src` to verify coverage targets.
- All tests must pass, including the new concurrency and failure-mode tests.
