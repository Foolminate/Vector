---
id: 6.4
title: "Deep Synthesis: Evaluation Strategy & Data Preservation"
type: issue
status: completed
labels: [ai, evaluation, data-integrity, enhancement]
---

> *This was generated by AI during triage.*

## Agent Brief

### Context
This is the final AI processing stage. Agent 2 (Evaluation) performs a deeper qualitative analysis on jobs that passed the initial Triage (Agent 1). We need to move this into the new `AgentPipeline` and fix issues around data preservation and review overrides.

### What to build
1. **EvaluationStrategy (src/pipeline.py):**
   - Implements `BaseStrategy`.
   - Defines a deep analysis prompt and a complex Pydantic schema (`EvaluationResult`).
   - Schema should include: `suitability_score`, `pros`, `cons`, `verdict` ('shortlisted', 'edge-case', 'discarded').
2. **Data Preservation:**
   - Ensure `JobRepository` has a method to save this deep analysis into `analysis_json`.
   - **Crucial:** Even if the verdict is 'discarded', the `analysis_json` MUST be saved (Fix Note 10).
3. **Force Evaluation (src/database.py):**
   - Implement `JobRepository.reset_for_evaluation(job_id)` which sets status back to 'high-pass' (or equivalent) to trigger Agent 2 again.
   - This fixes the "review override" bug (Fix Note 1).

### Implementation Notes
- **Strategy Chain:** While not strictly required to be automatic, the design should allow Agent 2 to be triggered after Agent 1 finishes.
- **Complexity:** Agent 2 uses a more expensive model (e.g., `gemini-3.1-pro-preview`). The `ModelAdapter` already handles model names.

### Test Strategy
- **Unit Test:** Create `tests/test_evaluation_refactor.py`.
- **Preservation Test:** Verify that `analysis_json` is not NULL after a 'discarded' evaluation.
- **Override Test:** Verify that `reset_for_evaluation` enables a second run of the pipeline for a job.

## Parent
PRD: Architectural Refactoring for Deep Modules & Pipelined Intelligence (6.0)

## What to build
Implement the `EvaluationStrategy` for deep qualitative synthesis and ensure all analysis data is preserved. Fix the Agent 2 review override and ensure rejected evaluation data is saved.

## Acceptance criteria
- [x] `EvaluationStrategy` implements the prompt and Pydantic schema.
- [ ] `analysis_json` is preserved for all evaluated jobs (Fix Note 10).
- [ ] Implement a functional `force_evaluate(job_id)` method (Fix Note 1).
- [ ] The pipeline handles Agent 2 synthesis as jobs exit the Triage stage.

## Blocked by
- 6.3: Pipelined Triage: Model Adapter & Triage Strategy
