---
id: 7.4
title: Integrate LLM retry visibility into audit_log
type: issue
status: needs-triage
labels: [qa, llm, logging]
---

## Parent
.scratch/project-vector/7.0-qa-robustness.md

## What to build
Modify the `ModelAdapter` in `src/llm_client.py` to record `tenacity` retry attempts in the database `audit_log`. This ensures that API instability is visible to system administrators through the standard audit trail.

## Acceptance criteria
- [ ] `log_retry` hook in `ModelAdapter` updated to call `self.repo.log_action`.
- [ ] Retry logs include attempt number and exception summary.
- [ ] Verification via updated `tests/test_llm_client.py` that retry actions appear in the DB.

## Blocked by
None - can start immediately
