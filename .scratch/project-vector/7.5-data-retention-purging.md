---
id: 7.5
title: Implement 30-day data retention purging in JobRepository
type: issue
status: needs-triage
labels: [qa, database, maintenance]
---

## Parent
.scratch/project-vector/7.0-qa-robustness.md

## What to build
Implement a `purge_expired_jobs` method in `JobRepository` to automatically remove job records and associated metadata that are older than 30 days, ensuring compliance with the system's data retention policy.

## Acceptance criteria
- [ ] `JobRepository.purge_expired_jobs()` method implemented.
- [ ] SQL logic correctly targets jobs older than 30 days based on `created_at` or `expiration_date`.
- [ ] Integration into the main pipeline or a dedicated maintenance CLI command.

## Blocked by
None - can start immediately
