---
id: 7.3
title: Implement Scraper resilience & Bot-detection handling
type: issue
status: needs-triage
labels: [qa, scraper, robustness]
---

## Parent
.scratch/project-vector/7.0-qa-robustness.md

## What to build
Improve the `SeekCollector` to handle individual job failures gracefully by logging them to the `audit_log` rather than swallowing them. Implement an explicit bot-detection check that halts the scraper with a clear audit entry if Seek blocks the request.

## Acceptance criteria
- [ ] `SeekCollector.scrape_job_details` logs exceptions to `audit_log` with specific job URLs.
- [ ] Bot-detection check implemented at the start of `scrape()`.
- [ ] Scraper halts and logs `action='bot_blocked'` when detected.
- [ ] Updated `tests/test_collector.py` with mocked failure scenarios.

## Blocked by
- .scratch/project-vector/7.2-database-validation.md
