---
id: 7.1
title: Database Migration v10 & Schema Standardization
type: issue
status: completed
labels: [qa, database, migration, architecture]
---

## Parent
[.scratch/project-vector/7.0-qa-robustness.md](./7.0-qa-robustness.md)

## What to build
Implement database migration `v10` to prepare for robustness adjustments. This includes adding performance indices, unifying the `rationale` column into `analysis_json`, and ensuring the `jobs` table supports the `archived` status and TTL-based purging.

## Acceptance criteria
- [X] `migrations/v10_robustness_foundation.sql` created and applied.
- [X] Indices added to `seek_job_id` and `created_at`.
- [X] Existing `rationale` data migrated to `analysis_json` (where applicable).
- [X] `JobRepository` updated to use `discarded` instead of `rejected`.

## Blocked by
None - can start immediately
