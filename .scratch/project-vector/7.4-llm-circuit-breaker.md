---
id: 7.4
title: LLM Retry Visibility & Circuit-Breaker Implementation
type: issue
status: needs-triage
labels: [qa, llm, robustness]
---

## Parent
.scratch/project-vector/7.0-qa-robustness.md

## What to build
Enhance `ModelAdapter` with a circuit-breaker and audit logging. Record all `tenacity` retries in `audit_log`. If max retries are exceeded, set the job's `processing_status` to `failed-permanent` to prevent infinite loops in the pipeline.

## Acceptance criteria
- [ ] `log_retry` hook logs attempt number and error to DB.
- [ ] `ModelAdapter` catches final failure and updates `processing_status`.
- [ ] Tests verify that `failed-permanent` jobs are skipped in subsequent runs.

## Blocked by
- .scratch/project-vector/7.1-database-migration-v10.md
