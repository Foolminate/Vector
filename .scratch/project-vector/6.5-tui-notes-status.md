---
id: 6.5
title: "Human Feedback: TUI Note-Taking & Status Persistence"
type: issue
status: needs-triage
labels: [tui, data-integrity, AFK]
---

## Parent
PRD: Architectural Refactoring for Deep Modules & Pipelined Intelligence (6.0)

## What to build
Refactor the Review TUI to use the `JobRepository` for all state management. This ensures notes are saved correctly and statuses remain consistent between the TUI and the background engine.

## Acceptance criteria
- [ ] TUI uses `JobRepository.add_note()` to reliably persist human notes (Fix Note 4).
- [ ] Status transitions in the TUI (e.g., Shortlist, Discard) use the Repository's state machine (Fix Note 8).
- [ ] Visual highlighting is added for "New" items (Fix Note 5).

## Blocked by
- 6.1: Foundation: Typed Config & Base Job Repository
