---
id: 6.5
title: "Human Feedback: TUI Note-Taking & Status Persistence"
type: issue
status: ready-for-agent
labels: [tui, data-integrity, AFK]
---

> *This was generated by AI during triage.*

## Triage Notes
- **Status:** Completed.
- **Architectural Shift:** Transitioned to "Deep Repository" pattern. All TUI and Evaluator state changes are now encapsulated in `JobRepository`.
- **Data Integrity:** Notes now reliably update `last_decision_by = 'human'`.
- **Resilience:** Removed all numeric indices from TUI and Evaluator; replaced with key-based access.
- **Verification:** Verified via `tests/test_tui_integration.py` and `tests/test_evaluator.py`.

## Parent
PRD: Architectural Refactoring for Deep Modules & Pipelined Intelligence (6.0)

## What to build
Refactor the Review TUI to use the `JobRepository` for all state management. This ensures notes are saved correctly and statuses remain consistent between the TUI and the background engine.

## Acceptance criteria
- [x] TUI uses `JobRepository.add_note()` to reliably persist human notes (Fix Note 4).
- [x] Status transitions in the TUI (e.g., Shortlist, Discard) use the Repository's state machine (Fix Note 8).
- [x] Visual highlighting is added for "New" items (Fix Note 5).

## Blocked by
- 6.1: Foundation: Typed Config & Base Job Repository
