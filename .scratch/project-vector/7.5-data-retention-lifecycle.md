---
id: 7.5
title: Two-Stage Data Retention (Archive & Purge)
type: issue
status: completed
labels: [qa, database, maintenance, enhancement]
---

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**
- `JobRepository` needs a maintenance method `archive_and_purge()`.
- Stage 1: Jobs > 30 days old with status like 'analyzed', 'discarded', or 'edge-case' should be moved to 'archived'.
- Stage 2: Jobs > 90 days old should be hard-deleted.
- Hard-delete should ideally be cascaded or manually handle related `audit_log` entries if necessary (though `audit_log` isn't strictly foreign-keyed in the schema, we should check for linked details).
- The `jobs` table has a `created_at` column (indexed in v10) which is suitable for this logic.

**Recommendation:**
- Category: `enhancement`
- State: `ready-for-agent`

## Parent
[.scratch/project-vector/7.0-qa-robustness.md](./7.0-qa-robustness.md)

## What to build
Implement the `archive_and_purge()` method in `JobRepository`. This implements a two-stage lifecycle: jobs > 30 days old are marked as `archived` (soft-delete), and jobs > 90 days old are hard-deleted from the database.

## Acceptance criteria
- [x] `JobRepository.archive_and_purge()` implemented.
- [x] Soft-delete logic updates status to `archived`.
- [x] Hard-delete logic removes records from `jobs` and `audit_log` (where linked).
- [x] Logic verified via time-shifted test data.

## Blocked by
- [.scratch/project-vector/7.1-database-migration-v10.md](./7.1-database-migration-v10.md)

## Context Reset Note
**STOP AFTER COMPLETING THIS ISSUE.** 
This completes the **Logic & Schema Implementation Phase**. Reset the context window before moving to the intensive **Testing & Validation Phase**.

### Jump-Start Prompt
"The Core Implementation Phase for QA Robustness (7.1 - 7.5) is complete. The database is migrated, models are enforced, and the retention/circuit-breaker logic is live. Start **Issue 7.6: Advanced Test Backfill** to verify concurrency and edge cases. Refer to the testing decisions in `.scratch/project-vector/7.0-qa-robustness.md`."
