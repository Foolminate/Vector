---
id: 7.7
title: Integrated Pipeline Resilience Test (Full Lifecycle)
type: issue
status: ready-for-agent
labels: [enhancement, ready-for-agent, qa, testing, integration]
---

> *This was generated by AI during triage.*

## Parent
[.scratch/project-vector/7.0-qa-robustness.md](./7.0-qa-robustness.md)

## Triage Notes
**What we've established so far:**
- This is the final integration stage for the QA & Robustness work.
- It requires mocking both Playwright (scraper) and LLMClient (sorter/evaluator) to simulate a full run.
- The test must verify that the "resilience" features (bot detection, circuit breaker, audit logging) work in concert.

## Agent Brief

### Context
We need a high-fidelity end-to-end test that simulates the entire job processing pipeline under duress.

### Objectives
1. **Full Lifecycle Simulation:** Create `tests/test_pipeline_e2e.py` which triggers `SeekCollector`, `Sorter`, and `Evaluator` sequentially.
2. **Mocked Failures:**
    - Simulate a 403 Forbidden response in `collector.py` and verify the job status and audit log.
    - Simulate an LLM failure that triggers the circuit breaker, resulting in `failed-permanent`.
3. **Audit Trail Verification:** Ensure that the `audit_log` table contains a traceable sequence of events for a single job through all stages.
4. **State Consistency:** Verify that jobs move correctly from `new` -> `high-pass` -> `discarded` or `archived` as per the test scenario.

### Target Files
- `tests/test_pipeline_e2e.py` (New)

### Verification
- Run `pytest tests/test_pipeline_e2e.py`.
- Verify database state after the test run matches the expected outcomes for each mocked scenario.

